syntax="proto3";

package model;

message Quest{
    ///在任务表中的id
    int64 Id = 1;
    ///完成进度
    int64 Progress = 2;
    ///是否已领取奖励
    bool Received = 3;
    ///是否已完成
    bool Completed = 4;
    ///任务类型
    int32 Type = 5;
}
