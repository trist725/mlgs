syntax="proto3";

package model;

message Achievement{
    ///在成就表中的id
    int64 Id = 1;
    ///对应成就任务在任务表中的id
    int64 TaskId = 2;
    ///是否已完成
    bool Completed = 3;
    ///成就表中的类型
    int32 Type = 4;
}