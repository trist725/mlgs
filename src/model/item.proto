syntax="proto3";

package model;

/// 用户物品信息
message Item {
    /// 唯一id
    string UID = 1;
    /// 类型id, item.xlsx中的id字段
    int32 TID = 2;
    /// 创建时刻
    int64 CreateTime = 3;
    /// 数量
    int32 Num = 4;
}
